你使用了哪些 AI 工具？如何使用的？
我是用的是codebuddy工具
对于本项目我是用ai工具的顺序是：
1.先让ai阅读.md文件，了解本项目ai开发的规范以及任务清单
2.根据任务清单，分前后端实现相关功能
3.在修改代码前，我会让ai给我一个方案，我确定了方案之后再让ai对代码进行修改，这样可以减少ai乱改的可能性
4.在修改后端代码时候，我会要求ai按照api_spec.md文件，生成对应的接口文档
5.在ai写完后端代码之后，我会要求ai对刚刚所写的代码进行接口测试，要求所有情况测试通过后才算完成
6.完成后让ai提交到git中
7.对于前端也是一样的操作

遇到了什么问题？如何解决的？

问题1：库存查询页面输入内容后自动触发查询
- 原因：初始实现使用 watch 监听搜索条件变化，输入即触发，不符合交互需求
- 解决：移除 watch 监听，将查询按钮绑定到带防抖的 handleSearch 函数，点击按钮才触发

问题2：商品删除未校验关联库存
- 原因：ProductService.delete() 只检查商品是否存在，未检查关联库存，删除后数据孤立
- 解决：在 InventoryRepository 新增 existsByProductId() 方法，删除前检查，如有关联则抛出异常

问题3：商品编辑后页码重置
- 原因：handleSubmit 中硬编码 currentPage.value = 1
- 解决：删除该行，保持当前页码不变

问题4：商品删除失败时无错误弹窗
- 原因：catch 块未处理错误，只处理了取消操作
- 解决：添加 ElMessage.error() 显示后端返回的错误信息

如果有更多时间，你还会做什么？
如果还有时间我会用ai写一些测试用例，这个是ai能够帮助开发提升效率比较直接的体现了
